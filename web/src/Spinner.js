import React from 'react';
import './Spinner.css'; // Asegúrate de que el CSS del spinner esté aquí

const Spinner = () => {
  return <div className="spinner"></div>;
};

export default Spinner;
